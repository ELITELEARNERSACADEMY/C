#include <stdio.h>

// we will be using the predefined function
// predefined  functions are included in labraries 
// example string has len(), strccmp(),strcpy(),strncpy() etc

int main(){
char Name[15];

printf("What is your name: ");// outputting function 
scanf("%s",&Name); // inputting information using scanf function

printf("Hello %s are doing so well "); // output Hello yourName are doing so well 

//other predefined functions 
//fget() inputting function ' strings and writing  to a file '
//gets() inputting function
//fput() writting to a file 
//fwrite()
//they are may more predefined function  


return 0;
}
